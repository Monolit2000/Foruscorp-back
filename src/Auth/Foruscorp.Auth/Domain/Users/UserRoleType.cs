namespace Foruscorp.Auth.Domain.Users
{
    public enum UserRoleType
    {
        Admin,
        Driver,
        Manager, //Admin
        SuperAdmin,
        SelfDriver, //Self-registered driver
    }
}
